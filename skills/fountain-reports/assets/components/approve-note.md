<!-- Data: none. Pick the note that matches what approval actually does for this show. -->
<!-- Written as a quote so it is one element, which is how the email stylesheet finds it. -->
<!-- The first is for a report sent before any render, when a render machine works this show. -->
<!-- The second is for a report sent before any render when no machine does - one laptop and no -->
<!-- scheduler is the common case, and there the first note is untrue. -->
<!-- The third is for a report sent after the render, which is the default: the clips already exist. -->
<!-- Approving is not a dashboard-only act: say so, because the reader can simply tell you. -->

> **Approving a post is what makes it render.**
> Tell me which ones, or approve them in the dashboard, and the finished clips follow in the next email.

> **Approving a post does not render it here.**
> Nothing on this machine watches for an approval, so tell me which ones and I will approve them and render.

> **The clips are made, and approving one is what sends it.**
> Tell me which to publish now and which to publish later, and at what time, and I will set it.
